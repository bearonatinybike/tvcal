See [AGENTS.md](AGENTS.md) — this project keeps its agent guidance there, so it
is picked up by any tool that reads the shared `AGENTS.md` convention, not just
Claude Code. This file is only a pointer; don't add guidance here.
